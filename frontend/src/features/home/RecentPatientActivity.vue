<template><ClinicalCard title="Recent patient activity"><div class="activity"><article v-for="event in events" :key="event.id"><time>{{ event.date }}</time><div><h3>{{ event.title }}</h3><p>{{ event.summary }}</p></div><ProvenanceBadge :provenance="event.provenance" /></article></div></ClinicalCard></template>
<script setup lang="ts">import type { TimelineEvent } from '../../types/patient360'; import ClinicalCard from '../../components/ui/ClinicalCard.vue'; import ProvenanceBadge from '../../components/ui/ProvenanceBadge.vue'; defineProps<{ events: TimelineEvent[] }>();</script>
<style scoped>.activity { display: grid; gap: 12px; }.activity article { display: grid; grid-template-columns: 90px 1fr auto; gap: 12px; border-bottom: 1px solid var(--color-line); padding-bottom: 12px; }.activity article:last-child { border-bottom: 0; }.activity time { color: var(--color-muted); font-size: 14px; }.activity h3 { margin: 0; font-size: 1rem; }.activity p { margin: 3px 0; color: var(--color-muted); font-size: 14px; } @media (max-width: 580px) { .activity article { grid-template-columns: 1fr; gap: 5px; } }</style>
