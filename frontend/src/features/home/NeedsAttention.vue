<template><ClinicalCard title="Needs attention"><div class="attention"><article v-for="item in items" :key="item.id"><div><StatusPill :label="item.severity === 'review' ? 'Review' : item.severity === 'warning' ? 'Warning' : 'Urgent'" :tone="item.severity === 'urgent' ? 'danger' : item.severity === 'warning' ? 'warning' : 'neutral'" /><h3>{{ item.title }}</h3><p>{{ item.detail }}</p></div><ProvenanceBadge :provenance="item.provenance" /></article></div></ClinicalCard></template>
<script setup lang="ts">import type { AttentionItem } from '../../types/patient360'; import ClinicalCard from '../../components/ui/ClinicalCard.vue'; import StatusPill from '../../components/ui/StatusPill.vue'; import ProvenanceBadge from '../../components/ui/ProvenanceBadge.vue'; defineProps<{ items: AttentionItem[] }>();</script>
<style scoped>.attention { display: grid; gap: 12px; }.attention article { display: flex; justify-content: space-between; gap: 14px; border-bottom: 1px solid var(--color-line); padding-bottom: 12px; }.attention article:last-child { border-bottom: 0; }.attention h3 { margin: 8px 0 2px; font-size: 1rem; }.attention p { margin: 0; color: var(--color-muted); font-size: 14px; }</style>
