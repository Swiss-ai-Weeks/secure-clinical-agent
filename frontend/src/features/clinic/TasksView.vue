<template><section class="tasks"><header><p>Clinical operations</p><h1>Tasks & follow-ups</h1></header><div class="task-list"><article v-for="task in tasks" :key="task.description"><div><StatusPill :label="task.priority" :tone="task.priority === 'High' ? 'danger' : 'neutral'" /><h2>{{ task.description }}</h2><p>{{ task.patient }} · due {{ task.dueDate }} · {{ task.assignee }}</p><small>{{ task.source }} · {{ task.status }}</small></div><div v-if="task.source.includes('AI')" class="actions"><button type="button" @click="task.status = 'Confirmed'">Confirm task</button><button type="button" @click="task.status = 'Dismissed'">Dismiss</button></div></article></div></section></template>
<script setup lang="ts">import { reactive } from 'vue'; import StatusPill from '../../components/ui/StatusPill.vue'; const tasks = reactive([{ patient: 'Elisabeth Keller', description: 'Review HbA1c trend for p_101', dueDate: '2026-09-14', assignee: 'Dr. Sarah Chen', priority: 'High', status: 'Pending', source: '✦ AI suggested' }, { patient: 'Elisabeth Keller', description: 'Repeat HbA1c in three months', dueDate: '2026-12-18', assignee: 'Nurse Alex Rivera', priority: 'Normal', status: 'Needs confirmation', source: 'Clinical plan' }, { patient: 'Maria Santos', description: 'Confirm booked cardiology appointment', dueDate: '2026-10-02', assignee: 'Diego Santos', priority: 'High', status: 'Pending', source: 'Patient questionnaire' }]);</script>
<style scoped>.tasks { display: grid; gap: 20px; max-width: 1000px; }.tasks header p { margin: 0; color: var(--color-muted); font-size: 14px; font-weight: 800; text-transform: uppercase; }.tasks h1 { margin: 4px 0; font-size: clamp(2rem, 4vw, 3rem); }.task-list { display: grid; gap: 12px; }.task-list article { display: flex; justify-content: space-between; gap: 18px; border: 1px solid var(--color-line); border-radius: var(--radius-card); background: var(--color-surface); padding: 20px; }.task-list h2 { margin: 10px 0 4px; font-size: 1.15rem; }.task-list p, .task-list small { color: var(--color-muted); }.task-list p { margin: 0; }.task-list small { display: block; margin-top: 6px; }.actions { display: flex; flex-wrap: wrap; align-content: start; gap: 8px; }.actions button { border: 1px solid var(--color-line); border-radius: var(--radius-control); background: var(--color-surface); padding: 0 12px; cursor: pointer; }.actions button:first-child { background: var(--color-plum); color: white; }</style>
