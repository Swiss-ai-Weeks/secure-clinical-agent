# Main schema integration checkpoint

Merged main's schema commits `1e20850a17a93054ecf449ea4a92358f910cf3b6` and `8465fcb` on 2026-09-17 without Git conflicts. After reviewing the differences below, the user chose birth year only, temporary raw non-patient source resource IDs, and replacement of the current demo PostgreSQL volume. The schema has now been initialized and validated on this server; that does not populate the clinical seed.

The colleague's [FHIR resource plan](../../docs/Patient360-FHIR-Resource-Plan.md), [build plan](../../docs/Patient360-Build-Plan.md), and [SQL schema](../deploy/patient360/sql/fhir/) inform the ingestion projection. They add `clinical`, `identity`, and `audit` schemas, resource field allowlists, component observations, patient-scoped foreign keys, source uniqueness, citations, and separate application roles. The `identity` schema holds application users and hashed sessions, not the ingestion source-to-opaque registry; run tokens are stateless JWTs bound to sessions. PostgreSQL audit replaces immudb as the default deployment; immudb is under the `legacy` profile. The existing legacy container was not removed by this operation.

## Reconciled choices and remaining adapter work

| Area | Imported main contract | User decision / next work |
| --- | --- | --- |
| Synthetic birth dates | `clinical.patients` has `birth_year`; its JSONB constraint accepts only a four-digit birthDate. | Adopt birth year only, superseding the prior full-DOB choice. Keep full DOB out of prepared records, notes, and embedding payloads; immutable restricted source FHIR remains unchanged. |
| Source identity | Non-patient clinical tables store unique raw `ResourceType/id` strings in `source_id`, without a source namespace. | Accept these internal upsert keys temporarily within the agreed source namespace. Patient keys and application citations remain opaque. [Move source resource identifiers into restricted provenance](../../.scratch/ingestion-pipeline/issues/12-restrict-source-resource-identifiers.md) tracks removal of the exception. |
| Migration and lifecycle | Init scripts run on empty volumes; worker has insert/update, but no clinical delete privilege. | The user authorized the current volume replacement, performed after backup. Future retry/update/delete behavior remains to specify; the worker must not reset volumes or gain delete privileges by implication. |
| Key representation and citations | Patient keys must match `p_[0-9a-z]+`; resource UUIDs and short random `cite_id` values are separate. | Encode registry-issued opaque patient identities in the schema's accepted format, keep citations stable on replay, and retain resource lineage separately. A plain hyphenated UUID cannot be inserted as patient_key. |
| Scope and note policy | Schema covers more resources than the initial cohort projection; the broader plan caps ages at 90+ and describes vault integration. | Initial ingestion scope remains recorded in the cohort ticket. Apply the schema's read-time age policy; specify minimum Encounter linkage, narrative policy, and restricted mapping access in the existing tickets. Importing the schema does not implement every resource adapter. |

The three publication-blocking differences are resolved by the user. [Choose the FHIR-to-PostgreSQL projection and update rules](../../.scratch/ingestion-pipeline/issues/05-fhir-projection.md) still owns the adapter's exact projection, labels, coding/components, and update rules; note sanitization, grants, and lifecycle retain their tickets. The source worker still stops at `source_ready`.

## Runtime replacement evidence

- Before replacement: the old `fhir` database had four empty public clinical tables and no other active database clients.
- Backups: a complete logical cluster dump and a custom-format `fhir` archive are retained under restricted ignored directory `.data/pg-rebuild-2092py9w/`. Checksums are in [local execution evidence](../../.data/pg-rebuild-2092py9w/execution.json). Restoring the custom archive into an isolated database recovered the four original tables successfully.
- Preflight: initialized the current SQL in an isolated temporary PostgreSQL container and verified role logins, constraints, and privileges before replacing the live volume.
- Replacement: recreated only `patient360_postgres-fhir-data` and `patient360-postgres`, using the maintained Compose definition and its `.env` credentials. The former container's administrator password differed from the maintained `.env`; the rebuilt database now uses the maintained value. New application-role credentials were generated into the ignored owner-restricted `.env`, never committed.
- Result: PostgreSQL is healthy; `clinical` has 16 tables, `identity` has 2, and `audit` has 2. Worker/app/auditor password logins pass. Worker clinical and audit inserts succeed; full DOB and cross-patient encounter links are rejected; worker delete, app clinical writes, and auditor clinical reads are denied. Test rows were rolled back, leaving patient/observation/audit-event tables empty.
- Other service container IDs and source artifacts were preserved. The temporary probe container and temporary credential file were removed. This validates database bootstrap and the selected checks, not end-to-end ingestion or search.
