<template><section class="audit"><header><p>Governance</p><h1>Audit & privacy</h1><span>Every AI answer is scoped to the records your role may access.</span></header><div class="audit__labels"><StatusPill label="AI query access" tone="warning" /><StatusPill label="Patient record access" tone="neutral" /><StatusPill label="Blocked access" tone="danger" /></div><div class="audit__table"><article v-for="row in auditRows" :key="`${row.time}-${row.actor}`"><time>{{ row.time }}</time><strong>{{ row.actor }}</strong><span>{{ row.action }}</span></article></div></section></template>
<script setup lang="ts">import StatusPill from '../../components/ui/StatusPill.vue'; const auditRows = [{ time: '17:04', actor: 'Dr. Müller', action: 'Viewed Emma Laurent → Laboratory results' }, { time: '16:52', actor: 'AI Assistant', action: 'Accessed 3 records to answer clinical query' }, { time: '16:44', actor: 'Access denied', action: 'Restricted document requested' }];</script>
<style scoped>.audit { display: grid; gap: 20px; max-width: 1000px; }.audit header p { margin: 0; color: var(--color-muted); font-size: 14px; font-weight: 800; text-transform: uppercase; }.audit h1 { margin: 4px 0; font-size: clamp(2rem, 4vw, 3rem); }.audit header span { color: var(--color-muted); }.audit__labels { display: flex; flex-wrap: wrap; gap: 8px; }.audit__table { border: 1px solid var(--color-line); border-radius: var(--radius-card); background: var(--color-surface); overflow: hidden; }.audit__table article { display: grid; grid-template-columns: 80px 150px 1fr; gap: 16px; border-bottom: 1px solid var(--color-line); padding: 18px; }.audit__table article:last-child { border-bottom: 0; }.audit time, .audit span { color: var(--color-muted); } @media (max-width: 600px) { .audit__table article { grid-template-columns: 1fr; gap: 4px; } }</style>
